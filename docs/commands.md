# BookFlow Commands

Use `help` inside the application to see the command list.

Common commands:

```text
book add
book list
reader add
reader list
loan create
loan return <bookId>
reservation create
search <text>
filter available
stats
report
exit
```
